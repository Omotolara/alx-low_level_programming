#include "main.h"

/**
  * mul - Multiplies two integers
  * @a: first number to multiply
  * @b: second number to multiply with first number
  *
  * Return: multiplication of a param and b param
  */
int mul(int a, int b)
{
	return (x * y);
}
